[[smoking-tigers-ai-buildout]]
[[mac-mini-phase1-setup]]
smb://eds-mac-mini.tailebe6d3.ts.net
https://github.com/edjieun/openclaw
https://github.com/edjieun/smoking-tigers-governance

openrouter starting amount: $8.77
- added $50
- current: $58.77

# update the project
[[smoking-tigers-ai-buildout]]
# Clean up the cruft

Our todo list:
- Setup `search_memory` tool to work as intended
    - We need that embeddings model!
- Remove all the skills
- Remove useless tools


# Kill the skills

Openclaw comes with a bunch of bad defaults. Skills are one of them. They might not be useless, but most likely using a smaller model they are just context spam.

Disable all active skills.

# Prune Tools

Disable the following tools:

- canvas
- nodes
- gateway
- agents_list
- sessions_list
- sessions_history
- sessions_send
- sessions_spawn
- sessions_status
- web_search
- browser

Some of these rely on external services, other are just probably too complex for a model you can self host. This does basically kill most of the bots "self-awareness" but that really just is a self-fork-bomb trap.

# Forked Repos to Customize

https://github.com/edjieun/pinchy
https://github.com/edjieun/zeroclaw
https://github.com/edjieun/hermes-agent
https://github.com/edjieun/mcp-searxng
https://github.com/edjieun/playwright-mcp
https://github.com/edjieun/openclaw-nerve
https://github.com/edjieun/hermesclaw
https://github.com/edjieun/mcp-server-youtube-transcript
https://github.com/edjieun/mcp-crawl4ai-ts
https://github.com/edjieun/enchanted
https://github.com/edjieun/code-gov
https://github.com/edjieun/guides-template

I have forked several repos to use when customizing the mac mini.  not all are needed.  some have been tested and how we use them should be updated in our gov repo.

hermesagent was setup to be used as an mcp.  i want the ability to learn. using a small model.  usage needs to be defined.

pinchy - the way i was trying to use openclaw, should this be added?

zeroclaw - will this increase performance the way I want to use it.

openclaw nerve - mattermost is not working for me.  openclaw dashboard isn't good.

# Mattermost
channels were added, none were deleted
maven and pmp agent bots need to be deleted.

---

# TigerClaw Phase 2 Implementation (2026-07-18)

## Done
- `openclaw.json` rewritten on Mac Mini — lean TigerClaw config
  - Provider key collision fixed: `lmstudio` → `lmstudio-mini` (was causing phantom cloud fallback)
  - `tools.profile: minimal`, `skills.allowBundled: []`
  - openrouter plugin disabled, mattermost plugin disabled
  - Hard local block: no cloud fallback on any model path
  - Kept: `memory-core` dreaming (3am Qwen), Hermes MCP, `searxng`, `duckduckgo`
  - Context window lowered to 16k (was 100k — stability fix)
- ZeroClaw v0.8.3 installed on Mini (`aarch64-apple-darwin`)
  - Config: `~/.zeroclaw/config.toml`
  - Memory: SQLite hybrid search (vector 0.7 + keyword 0.3)
  - Embedding: `custom:http://localhost:1234/v1` → nomic-embed via LM Studio (no external deps)
  - Tunnel: Tailscale
- OpenRouter balance: $57.06 (was $58.77 — ~$1.71 spent)

## ZeroClaw Scout — WORKING (end of 2026-07-18)
- `zeroclaw agent --agent scout -m "say hello"` → responds ✅
- Model: `qwen/qwen3.5-9b:2`, 32768 context, 1 parallel slot
- Root cause of n_keep errors: LM Studio GUI held old `defaultContextLength: 8192` in memory; closing GUI + restarting headless server with `lms server start` picked up updated `settings.json` (32768)
- Provider: `lmstudio`, provider alias: `lmstudio.default`, model alias: `lmstudio.default`
- ZeroClaw workspace: `~/.zeroclaw/agents/scout/workspace` (quickstart created this, not `~/.zeroclaw/workspace`)
- SOUL.md with STE identity at `~/.zeroclaw/workspace/SOUL.md` — needs to be copied to agent workspace

## Also completed end of session
- STE SOUL.md copied to agent workspace: `~/.zeroclaw/agents/scout/workspace/SOUL.md`
- `zeroclaw agent --agent scout -m "what is STE?"` → correct STE answer ✅
- LM Studio boot service installed: `~/Library/LaunchAgents/ai.lmstudio.server.plist` (starts server + loads Qwen on login)
- Nerve installed at `~/nerve`, running on port 3080, already wired to TigerClaw gateway (:18789)
- `mcp-crawl4ai-ts` cloned + built at `~/mcp-crawl4ai-ts`
- `mcp-searxng` cloned + built at `~/mcp-searxng`
- Both MCPs added to `~/.zeroclaw/config.toml`
- Node.js 22 installed via nvm

## Next session
- Open Nerve in browser: `http://100.104.149.107:3080` (via Tailscale) or `http://192.168.1.253:3080` (LAN)
- Design 4 kanban lanes in Nerve: transcript, email, url/web, message-forward
- Configure ZeroClaw email channel: `ed@quorum.one` IMAP
- Test mcp-crawl4ai-ts: `zeroclaw agent --agent scout -m "fetch and summarize https://github.com/edjieun/smoking-tigers-governance"`
- ~~Deactivate `mavin` + `pmp-team` MM bots~~ ✅ deactivated via API 2026-07-18
- ⚠️ Rotate `edlicious` MM admin password (exposed in Copilot chat — low priority but do it)

## Architecture decisions locked
- ZeroClaw = memory backend (not Hermes)
- Hermes = MCP skill layer (deferred)
- TigerClaw = source fork of openclaw (extensions cleanup next session)
- Nerve = UI/kanban (replacing Mattermost)
- Email: ed@quorum.one

