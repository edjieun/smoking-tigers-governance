[[smoking-tigers-ai-buildout]]

openrouter balance: $53.98 (added $50 on 2026-07-18)

# 2026-07-19 — System Reconnection

## What was broken (root causes identified)

1. **Scout ↔ Mattermost disconnected** — Phase 2 TigerClaw rebuild added `mattermost`
   to `plugins.deny` and zeroed out `channels.mattermost` config. Pipeline was dead.

2. **Nerve spinning on gateway** — Nerve was pointed at ZeroClaw gateway (`:42617`)
   which uses a different WS protocol than OpenClaw. Nerve expects OpenClaw protocol.

3. **memorySearch provider wrong** — was `lmstudio-mini` (ZeroClaw name), should be
   `lmstudio` (OpenClaw name).

## What was fixed (2026-07-19)

- [x] Scout ↔ Mattermost reconnected:
  - Removed `mattermost` from `plugins.deny`
  - Re-enabled `mattermost` in `plugins.entries`
  - Restored full `channels.mattermost` config (bot token, server URL, accounts)
  - Added `mattermost` to `plugins.allow`
  - Added Nerve origins to `gateway.controlUi.allowedOrigins`
  - **Verified**: Scout responded in `#transcripts` within seconds of test message
- [x] Nerve fixed:
  - Changed `GATEWAY_URL` from `:42617` (ZeroClaw) → `:18789` (OpenClaw)
  - Updated `GATEWAY_TOKEN` to OpenClaw gateway token
  - Nerve health: `gateway: "ok"`
- [x] memorySearch fixed: `lmstudio-mini` → `lmstudio`
- [x] `docs/mattermost-channels.md` written — team channel guide

## Next: Nerve browser fix

In Chrome, open DevTools console and run:
```javascript
localStorage.removeItem('oc-config'); location.reload();
```
This clears stale connection config. Nerve will auto-connect to OpenClaw gateway.

## System state (end of 2026-07-19)

```
Mac Mini
  OpenClaw gateway (:18789) ← Nerve UI connects here ✅
    Scout agent
      ↓ listens on
  Mattermost #transcripts ✅
      ↓ posts to
  Mattermost #tasks + #decisions ✅
      ↓ memory search via
  LM Studio nomic-embed (:1234) ✅

Nerve web UI (:3080) → points at OpenClaw gateway ✅
ZeroClaw (:42617) → memory backend (independent, not yet wired to pipeline)
```

## Still pending
- [ ] Nerve browser: `localStorage.removeItem('oc-config')` in Chrome DevTools
- [ ] Test full pipeline: post transcript → verify #tasks + #decisions populated
- [ ] Wire ZeroClaw memory into Scout pipeline (search_memory across sessions)
- [ ] Van + Basil MM onboarding (pending their readiness)
- [ ] ZeroClaw email channel: `ed@quorum.one` IMAP
- [ ] edlicious MM password rotation (low priority)
